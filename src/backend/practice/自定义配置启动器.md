---
title:  Spring Boot 自定义启动器：解决多环境与配置问题
date: 2025-6-12
author: JackSu
categories:
  - 工具
tags:
  - springboot
---

> springboot基于约定大于配置的理念，简化了应用程序的启动和开发过程。但在实际项目中，尤其是微服务架构下，往往需要对启动过程进行定制化处理，以满足不同环境和配置的需求。本文将介绍如何通过自定义 Spring Boot 启动器来解决这些问题，实现一次打包，多处运行。


在 Spring Boot 项目中，最常见的启动方式是通过 `SpringApplication.run()` 方法：

```java
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

}
```

这种方式简单直接，但一旦我们需要在启动过程中做定制化处理，例如：

- 定制启动 Banner
- 设置自定义监听器
- 统一环境变量（dev / test / prod）
- 扩展启动逻辑（注册中心、配置中心等）

就会显得不够灵活。

---

##  启动定制带来的问题

以「关闭启动 Banner」为例，Spring Boot 官方提供了两种方式。

### 方式一：使用 SpringApplication

```java
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication application = new SpringApplication(Application.class);
        application.setBannerMode(Banner.Mode.OFF);
        application.run(args);
    }

}
```

### 方式二：使用 SpringApplicationBuilder

```java
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        new SpringApplicationBuilder()
            .sources(Application.class)
            .bannerMode(Banner.Mode.OFF)
            .run(args);
    }

}
```

### 问题分析

- 启动代码冗余
- 微服务数量一多，维护成本极高
- 环境与配置分散在各工程中

---

## 自定义启动器

核心思路是基于 `SpringApplicationBuilder` 进行封装，统一处理：

- 环境变量
- 系统属性
- 启动扩展点

---

## 启动器核心实现

```java
public class BladeApplication {

    public static ConfigurableApplicationContext run(
            String appName, Class source, String... args) {

        SpringApplicationBuilder builder =
            createSpringApplicationBuilder(appName, source, args);
        return builder.run(args);
    }

    private static SpringApplicationBuilder createSpringApplicationBuilder(
            String appName, Class source, String... args) {

        Assert.hasText(appName, "服务名不能为空");

        ConfigurableEnvironment environment = new StandardEnvironment();
        MutablePropertySources propertySources = environment.getPropertySources();
        propertySources.addFirst(new SimpleCommandLinePropertySource(args));

        String[] activeProfiles = environment.getActiveProfiles();
        SpringApplicationBuilder builder =
            new SpringApplicationBuilder(source);

        String profile;
        if (activeProfiles.length == 0) {
            profile = "dev";
            builder.profiles(profile);
        } else if (activeProfiles.length == 1) {
            profile = activeProfiles[0];
        } else {
            throw new RuntimeException("多个环境冲突");
        }

        Properties props = System.getProperties();
        props.setProperty("spring.application.name", appName);
        props.setProperty("spring.profiles.active", profile);

        return builder;
    }
}
```

---

## 使用方式

```java
@SpringCloudApplication
public class DemoApplication {

    public static void main(String[] args) {
        BladeApplication.run("demo-service", DemoApplication.class, args);
    }
}
```

---

## 环境切换

```bash
java -jar app.jar --spring.profiles.active=prod --server.port=2333
```

---

## 📦 一次打包，处处运行

通过自定义启动器，可以做到：

- 只打包一次
- 运行时切换环境
- 与 Docker / Kubernetes 完美配合

---

## 总结

- 启动代码更优雅
- 环境治理更清晰
- 适合中大型微服务体系

